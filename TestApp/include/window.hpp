#pragma once

namespace window
{
    inline constexpr int WIDTH          = 1280;
    inline constexpr int HEIGHT         = 720;
    inline constexpr int LOGICAL_WIDTH  = WIDTH / 2;
    inline constexpr int LOGICAL_HEIGHT = HEIGHT / 2;
}